from read import print_entry, print_dataset
import numpy

def ca_digits_train(dataset):
    something = [0] * 10
    return something

def ca_digits_evaluate(dataset, something):
    successes, fails = 0, 0
    return successes, fails

def ca_faces_train(dataset):
    something = [0] * 1000
    return something

def ca_faces_evaluate(dataset, something):
    successes, fails = 0, 0
    return successes, fails