from read import print_entry, print_dataset
import numpy

def nb_digits_train(dataset):
    something = [0] * 10
    return something

def nb_digits_evaluate(dataset, something):
    successes, fails = 0, 0
    return successes, fails

def nb_faces_train(dataset):
    something = [0] * 1000
    return something

def nb_faces_evaluate(dataset, something):
    successes, fails = 0, 0
    return successes, fails